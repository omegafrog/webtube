extends base
include mixins/info
block content 
    div 
        form(method="GET")
            input(type="text",placeholder="Search by title" name="keyword")
            input(type="submit")
    div 
        each video in videos 
            +info(video)